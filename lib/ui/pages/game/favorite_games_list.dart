import 'package:flutter/material.dart';
import 'package:ludo_mobile/data/repositories/favorite_games_repository.dart';

class FavoriteGamesList extends StatelessWidget {
  final List<FavoriteGame> favorites;
  const FavoriteGamesList({Key? key, required this.favorites}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
